package com.lcyf.cloud.module.{{moduleName}}.biz.infrastructure.entity;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import com.ejlchina.searcher.bean.DbField;
import com.ejlchina.searcher.bean.SearchBean;
{{#useTenantBaseDO}}
import com.lcyf.cloud.framework.tenant.core.db.TenantBaseDO;
{{/useTenantBaseDO}}
{{^useTenantBaseDO}}
import com.lcyf.cloud.framework.mybatis.core.dataobject.BaseDO;
{{/useTenantBaseDO}}
import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;
import lombok.EqualsAndHashCode;
{{#hasDateFields}}
import java.time.LocalDateTime;
{{/hasDateFields}}
{{#hasBigDecimalFields}}
import java.math.BigDecimal;
{{/hasBigDecimalFields}}

/**
 * {{description}}实体
 *
 * 数据库表: {{tableName}}
 *
 * @author {{author}}
 * @since {{date}}
 */
@Data
@EqualsAndHashCode(callSuper = true)
@TableName("{{tableName}}")
@SearchBean(tables = "{{tableName}}")
@Schema(description = "{{description}}实体")
public class {{EntityName}}Do extends {{#useTenantBaseDO}}TenantBaseDO{{/useTenantBaseDO}}{{^useTenantBaseDO}}BaseDO{{/useTenantBaseDO}} {

    /**
     * 主键ID
     */
    @TableId(value = "id", type = IdType.ASSIGN_ID)
    @DbField("id")
    @Schema(description = "主键ID")
    private Long id;

    {{#fields}}
    /**
     * {{fieldDescription}}
     */
    @DbField("{{dbFieldName}}")
    @Schema(description = "{{fieldDescription}}")
    private {{fieldType}} {{fieldName}};

    {{/fields}}
}
