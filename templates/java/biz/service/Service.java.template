package com.lcyf.cloud.module.{{moduleName}}.biz.service;

import com.lcyf.cloud.framework.common.pojo.PageResult;
import com.lcyf.cloud.module.{{moduleName}}.api.pojo.dto.{{EntityName}}Dto;
import com.lcyf.cloud.module.{{moduleName}}.api.pojo.cmd.{{EntityName}}AddCmd;
import com.lcyf.cloud.module.{{moduleName}}.api.pojo.cmd.{{EntityName}}UpdateCmd;

import java.util.Map;

/**
 * {{description}} Service接口
 *
 * 业务职责:
 * 1. 定义{{description}}业务逻辑接口
 * 2. 声明事务边界
 * 3. 提供业务方法契约
 *
 * @author {{author}}
 * @since {{date}}
 */
public interface I{{EntityName}}Service {

    /**
     * 分页查询{{description}}
     *
     * 业务流程:
     * 1. 接收查询参数
     * 2. 调用Gateway层分页查询
     * 3. 返回分页结果
     *
     * @param paraMap 查询参数Map（支持BeanSearcher动态查询）
     * @return 分页结果集
     */
    PageResult<{{EntityName}}Dto> get{{EntityName}}Page(Map<String, Object> paraMap);

    /**
     * 根据ID获取{{description}}
     *
     * 业务流程:
     * 1. 校验ID有效性
     * 2. 查询数据
     * 3. 返回详情
     *
     * @param id {{description}}ID
     * @return {{description}}详情数据
     * @throws com.lcyf.cloud.framework.common.exception.ServiceException 记录不存在
     */
    {{EntityName}}Dto get(Long id);

    /**
     * 新增{{description}}
     *
     * 业务流程:
     * 1. 参数验证
     * 2. 业务规则校验
     * 3. 数据持久化
     *
     * @param addCmd 新增命令对象
     * @return 新创建记录的ID
     * @throws com.lcyf.cloud.framework.common.exception.ServiceException 业务异常
     */
    Long create({{EntityName}}AddCmd addCmd);

    /**
     * 修改{{description}}
     *
     * 业务流程:
     * 1. 检查记录是否存在
     * 2. 验证业务规则
     * 3. 更新数据
     *
     * @param updateCmd 更新命令对象
     * @throws com.lcyf.cloud.framework.common.exception.ServiceException 记录不存在或更新失败
     */
    void modify({{EntityName}}UpdateCmd updateCmd);

    /**
     * 删除{{description}}
     *
     * 业务流程:
     * 1. 检查是否可删除（关联数据检查）
     * 2. 逻辑删除记录
     *
     * @param id {{description}}ID
     * @throws com.lcyf.cloud.framework.common.exception.ServiceException 记录不存在或删除失败
     */
    void delete(Long id);
}
